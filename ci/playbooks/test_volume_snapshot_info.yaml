---

- name: Test Volume Snapshot Info
  hosts: localhost
  collections:
    - opentelekomcloud.cloud
  tasks:

    - name: Get volume snapshot info
      volume_snapshot_info:
      register: snapshot

    - name: Debug snapshots
      debug:
        var: snapshot
